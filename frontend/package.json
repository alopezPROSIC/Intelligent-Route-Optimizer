{
  "name": "prosic-frontend-wrapper",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "cd /app/artifacts/prosic && PORT=3000 pnpm exec vite --config vite.config.ts --host 0.0.0.0 --port 3000"
  }
}
